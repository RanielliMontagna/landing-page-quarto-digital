import { TitleBar } from 'components';
import { DivContato } from './contato.styles';

const Contato = () => {
  return (
    <DivContato>
      <TitleBar rota="contato" />
    </DivContato>
  );
};

export default Contato;
